<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!--
  Configuration file for AntRun.
  
  Use this file to override the default properties contained
  in build.xml. For example, to override the build.jar.filename
  property (which sets the name of the compiled jar file), add
  an element
  
  <jar>
    <filename>foo</filename>
  </jar>
  
  inside the <build> element.
  
  **Caveat emptor:** in some cases, changing this file and running ant
  again will result in Ant using a cached version of the old file! Run
  "ant dummy" to make sure that the correct data is being read. Otherwise
  you might need to run "ant clean" and start over.
-->
<build>
  <!-- Uncomment any of these if you want to override the build
       script's default values -->

  <!-- The project's name -->
  <name>Cornipickle</name>
  
  <!-- The project's version number -->
  <version>1.0.1</version>
  
  <!-- The project's author. Currently this only
  shows up in the footer of the Javadoc documentation. -->
  <author>Sylvain Hallé</author>
  
  <!-- The project's main class -->
  <mainclass>ca.uqac.lif.cornipickle.server.Main</mainclass>
  
  <!-- Output jar -->
  <jar>
    <filename>cornipickle.jar</filename>
  </jar>
  
  <!-- Redefine directories -->
  <srcdir>Source/Server/src</srcdir>
  <bindir>Source/Server/bin</bindir>
  <test>
    <srcdir>Source/ServerTest/src</srcdir>
    <bindir>Source/ServerTest/bin</bindir>
  </test>
  
  <!--
    Write JAR dependencies here.
  -->
  <dependencies>
    <dependency>
      <!--
      Apache Commons CLI. Used to parse command-line options.
      -->
      <name>Apache Commons CLI</name>
      <classname>org.apache.commons.cli.GnuParser</classname>
      <files>
        <zip>http://archive.apache.org/dist/commons/cli/binaries/commons-cli-1.3.1-bin.zip</zip>
      </files>
    </dependency>
    <dependency>
      <!--
      Bullwinkle. Used to parse Cornipickle statements.
      -->
      <name>Bullwinkle</name>
      <classname>ca.uqac.lif.bullwinkle.BnfParser</classname>
      <files>
        <zip>https://github.com/sylvainhalle/Bullwinkle/releases/download/v1.1.7/bullwinkle-1.1.7.zip</zip>
      </files>
    </dependency>
    <dependency>
      <!--
      wro4j. Used to minify JSON and JS
      -->
      <name>wro4j</name>
      <classname>ro.isdc.wro.model.resource.processor.impl.js.JSMinProcessor</classname>
      <files>
        <jar>https://wro4j.googlecode.com/files/wro4j-core-1.6.3.jar</jar>
        <jar>https://wro4j.googlecode.com/files/wro4j-runner-1.6.3-jar-with-dependencies.jar</jar>
      </files>
    </dependency>
    <dependency>
      <!--
      json-simple. Used to parse JSON messages
      -->
      <name>json-simple</name>
      <classname>org.json.simple.parser.JSONParser</classname>
      <files>
        <jar>https://json-simple.googlecode.com/files/json-simple-1.1.1.jar</jar>
      </files>
    </dependency>
  </dependencies>
</build>
